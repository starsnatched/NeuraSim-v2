# NeuraSim-v2

Simulation of C. elegans neural development with visualization.

## Usage

Run `python -m src.simulation` to generate data/network.json.

Serve the `visualization` directory and open index.html to view the network.
